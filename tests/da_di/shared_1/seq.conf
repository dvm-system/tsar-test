plugin = TsarPlugin
sources = main.c
copy = $sources include/main.h
sample = output.txt #< сравнить/взять за эталон
options = -O3 -o seq.exe
run = "$clang $sources $options && ${run_prefix}seq.exe >output.txt 2>&1" # результат: output.txt
