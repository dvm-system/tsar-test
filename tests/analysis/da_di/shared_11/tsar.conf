plugin = TsarPlugin
name = shared_11
sources = main.c
copy = $sources
sample = $copy output.txt
clean = $sample
options = -print-only=da-di -print-step=4 -finbounds-subscripts
run = "$tsar $sources $options >output.txt 2>&1"
