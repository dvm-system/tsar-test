plugin = TsarPlugin
name = interproc_7
sources = main.c
copy = $sources
sample = $copy output.txt
clean = $sample
options = -print-only=da-di -print-step=4 -fno-math-errno
run = "$tsar $sources $options >output.txt 2>&1"
