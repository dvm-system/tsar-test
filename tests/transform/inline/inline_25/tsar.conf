plugin = TsarPlugin
sources = main.c
copy = $sources
output = output.$platform.txt
sample = $copy $output
clean = $sample *.orig
options = -clang-inline -fno-show-source-location -fno-caret-diagnostics
run = "$tsar $sources $options 2>&1 | $perl -p $sys_dir/filters/output.pl >$output"
