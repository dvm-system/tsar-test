#include <stdio.h>

int main(int Argc, char **Argv) {

  {

    printf("%s\n", "Command line arguments aren't specified!");

    printf("%s\n", "Command line arguments aren't specified!");
    return 0;
  }
}
