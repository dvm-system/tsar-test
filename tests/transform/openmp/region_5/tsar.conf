plugin = TsarPlugin
sources = main.c
copy = $sources
sample = $copy output.txt
clean = $sample *.orig
options = -clang-openmp-parallel -foptimize-only=parallel
run = "$tsar $sources $options 2>&1 | $perl -p $sys_dir/output_filter.pl >output.txt"
